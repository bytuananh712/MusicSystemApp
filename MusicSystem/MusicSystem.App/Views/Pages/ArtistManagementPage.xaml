<Page x:Class="MusicSystem.App.Views.Pages.ArtistManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản lý nghệ sĩ"
      Background="Transparent">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🎤 Quản lý nghệ sĩ" 
                       FontSize="24" 
                       FontWeight="Bold"/>
            <TextBlock Text="Thêm, sửa, xóa nghệ sĩ trong hệ thống" 
                       FontSize="12" 
                       Foreground="Gray"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Toolbar -->
        <Border Grid.Row="1" 
                Background="#FFF8F9FA" 
                Padding="15"
                CornerRadius="5"
                Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBox x:Name="txtSearch" 
                             Width="300" 
                             Height="35"
                             VerticalContentAlignment="Center"
                             Padding="10,0"
                             FontSize="13"
                             BorderBrush="#FFDEE2E6"
                             BorderThickness="1"/>
                    <Button Content="🔍 Tìm kiếm" 
                            Width="100" 
                            Height="35"
                            Margin="10,0,0,0"
                            Background="#FF6C757D"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="btnSearch_Click"/>
                </StackPanel>

                <!-- Actions -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="btnAddArtist"
                            Content="➕ Thêm nghệ sĩ"
                            Height="35"
                            Padding="20,0"
                            Background="#FF28A745"
                            Foreground="White"
                            FontWeight="SemiBold"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="btnAddArtist_Click"/>

                    <Button x:Name="btnRefresh"
                            Content="🔄"
                            Width="35"
                            Height="35"
                            Margin="10,0,0,0"
                            Background="#FF17A2B8"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="btnRefresh_Click"
                            ToolTip="Làm mới danh sách"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="2" 
                BorderBrush="#FFDEE2E6" 
                BorderThickness="1"
                CornerRadius="5">
            <DataGrid x:Name="dgArtists"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      RowHeight="50"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      AlternatingRowBackground="#FFF8F9FA">

                <DataGrid.Columns>
                    <!-- Avatar -->
                    <DataGridTemplateColumn Header="Avatar" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Width="40" 
                                        Height="40" 
                                        CornerRadius="20"
                                        Background="#FF9B59B6"
                                        HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding ArtistName, Converter={StaticResource InitialConverter}}" 
                                               FontSize="16"
                                               FontWeight="Bold"
                                               Foreground="White"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Tên nghệ sĩ" 
                                        Binding="{Binding ArtistName}" 
                                        Width="200"
                                        FontWeight="SemiBold"/>

                    <DataGridTextColumn Header="Tiểu sử" 
                                        Binding="{Binding Biography}" 
                                        Width="*"
                                        MaxWidth="400"/>

                    <DataGridTemplateColumn Header="Trạng thái" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Status, Converter={StaticResource StatusToBrushConverter}}"
                                        CornerRadius="3"
                                        Padding="8,3">
                                    <TextBlock Text="{Binding Status}" 
                                               Foreground="White" 
                                               FontSize="11"
                                               HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Tạo lúc" 
                                        Binding="{Binding CreatedAt, StringFormat='dd/MM/yyyy'}" 
                                        Width="100"/>

                    <DataGridTemplateColumn Header="Thao tác" Width="250">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="✏️ Sửa" 
                                            Padding="10,3"
                                            Margin="2"
                                            Background="#FFFFC107"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            Click="btnEdit_Click"
                                            Tag="{Binding ArtistId}"/>

                                    <Button Content="🗑️ Xóa" 
                                            Padding="10,3"
                                            Margin="2"
                                            Background="#FFDC3545"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            Click="btnDelete_Click"
                                            Tag="{Binding ArtistId}"/>

                                    <Button Content="🔒 Ẩn" 
                                            Padding="10,3"
                                            Margin="2"
                                            Background="#FF6C757D"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            Click="btnDisable_Click"
                                            Tag="{Binding ArtistId}"
                                            Visibility="{Binding Status, Converter={StaticResource ActiveToVisibilityConverter}}"/>

                                    <Button Content="✅ Hiện" 
                                            Padding="10,3"
                                            Margin="2"
                                            Background="#FF28A745"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            Click="btnEnable_Click"
                                            Tag="{Binding ArtistId}"
                                            Visibility="{Binding Status, Converter={StaticResource DisabledToVisibilityConverter}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="3" 
                Background="#FFF8F9FA"
                Padding="15"
                Margin="0,15,0,0"
                CornerRadius="5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock x:Name="txtStatus" 
                           Text="Tổng: 0 nghệ sĩ"
                           FontSize="12"
                           Foreground="Gray"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>